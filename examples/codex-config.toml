# Codex CLI 0.134+ hook shape. Keep this observer-only: notatechbro writes
# human preview text to stderr and leaves stdout empty in default mode.

[features]
hooks = true

[[hooks.PreToolUse]]
matcher = "Bash"
hooks = [{ type = "command", command = "notatechbro", timeout = 5 }]
